interface:
  display_name: "Temu Listing Automation"
  short_description: "Orchestrate script-driven Temu draft listing from listing.yaml"
  default_prompt: "Run the validator and listing workflow scripts, dispatch their actions to the browser adapter, inspect returned evidence, and stop for confirmation before submission."
